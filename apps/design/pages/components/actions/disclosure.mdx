import { MdxLayout } from "@/components/layouts/mdx";
import { Playground } from "@/components/playground";
import { Props } from "@/components/props";
import * as components from "@wonderflow/react-components";

export const example = `<Disclosure summary="Click to expand">
  <Text maxWidth="60ch" dimmed={6}>
    Cow ricotta hard cheese. Port-salut dolcelatte pecorino who moved my
    cheese cheddar feta mozzarella camembert de normandie. Fondue fondue
    cheese triangles cauliflower cheese lancashire edam croque monsieur
    blue castello. Pecorino danish fontina ricotta cheddar emmental
    pepper jack.
  </Text>
</Disclosure>`;

export default ({ children }) => (
  <MdxLayout
    title="Disclosure"
    description="Interactive element to control the visiblity of content."
    showMeta
    features={[
      "Keyboard accessible",
      "Customizable icon position",
      "Multiple sizes",
      "Themes ready",
    ]}
    links={[
      {
        label: "ARIA authoring",
        url: "https://www.w3.org/TR/wai-aria-practices-1.2/#disclosure",
      },
    ]}
    hero={
      <Playground
        liveAreaProps={{
          horizontalAlign: "center",
          fill: true,
        }}
        scope={{ ...components }}
        code={example}
      />
    }
  >
    {children}
  </MdxLayout>
);

## Anatomy

```tsx highlight=1,4
import { Disclosure } from "@wonderflow/react-components";

export default () => {
  return <Disclosure summary="Click to expand">...</Disclosure>;
};
```

## API Reference

<Props
  properties={[
    {
      name: "open",
      type: ["boolean"],
      default: "false",
      required: false,
      dangerous: false,
      description: "Set the default state",
    },
    {
      name: "summary",
      type: ["ReactNode"],
      default: "",
      required: true,
      dangerous: false,
      description: "Set the content of trigger",
    },
    {
      name: "padding",
      type: ["boolean"],
      default: "true",
      required: false,
      dangerous: false,
      description: "Align the content with the trigger",
    },
    {
      name: "contentMaxHeight",
      type: ["string"],
      default: "",
      required: false,
      dangerous: false,
      description: "Set the max height for the content, enabling the scroll",
    },
    {
      name: "dimension",
      type: ['"small"', '"regular"', '"big"'],
      default: '"regular"',
      required: false,
      dangerous: false,
      description: "Set the element size",
    },
    {
      name: "iconPosition",
      type: ['"left"', '"right"'],
      default: '"left"',
      required: false,
      dangerous: false,
      description: "Set the position of the icon",
    },
    {
      name: "expandable",
      type: ["boolean"],
      default: "true",
      required: false,
      dangerous: false,
      description: "Set if the element can be expanded and collapsed",
    },
  ]}
/>

## Accessibility

Adheres to the [`disclosure` role requirements](https://www.w3.org/TR/wai-aria-practices-1.2/#disclosure).

<Props
  properties={[
    {
      name: "tab",
      description: "Move focus on the summary element or outside",
    },
    {
      name: "enter",
      description: "Expand or collapse the content",
    },
  ]}
/>
