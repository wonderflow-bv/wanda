import { MdxLayout } from "@/components/layouts/mdx";
import { Playground } from "@/components/playground";
import { Props } from "@/components/props";
import * as components from "@wonderflow/react-components";

export const example = `<CircularProgress value={70} showProgress />`;

export default ({ children }) => (
  <MdxLayout
    title="Circular progress"
    description="Indicates determinate loading status"
    showMeta
    features={["Multiple sizes", "Themes ready"]}
    hero={<Playground scope={{ ...components }} code={example} />}
    links={[
      {
        label: "ARIA design",
        url: "https://www.w3.org/TR/wai-aria-1.2/#progressbar",
      },
    ]}
  >
    {children}
  </MdxLayout>
);

## Anatomy

```tsx highlight=1,4
import { CircularProgress } from "@wonderflow/react-components";

export default () => {
  return <CircularProgress value={70} />;
};
```

## API Reference

<Props
  properties={[
    {
      name: "value",
      type: ["number"],
      default: "",
      required: true,
      dangerous: false,
      description: "Define the current progress value",
    },
    {
      name: "max",
      type: ["number"],
      default: "100",
      required: false,
      dangerous: false,
      description: "Set the max value equal to 100% of the bar",
    },
    {
      name: "dimension",
      type: ['"regular"', '"big"'],
      default: '"regular"',
      required: false,
      dangerous: false,
      description: "Set the element size",
    },
    {
      name: "showProgress",
      type: ["boolean"],
      default: "",
      required: false,
      dangerous: false,
      description: "Show the progress percentage",
    },
  ]}
/>

## Accessibility

Adheres to the [`progressbar` role requirements](https://www.w3.org/TR/wai-aria-1.2/#progressbar).
