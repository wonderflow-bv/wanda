import { MdxLayout } from "@/components/layouts/mdx";
import { Playground } from "@/components/playground";
import { PropsTable } from "@/components/props-table";
import { Props } from "@/components/props";
import { Checklist } from "@/components/checklist";
import * as Components from "@wonderflow/react-components";
import { Markdown } from "@/components/markdown";

export const example = `<SplitButton kind="secondary" label="Button" placement="right-start">
  <Menu>
    <Menu.Item padding={false}>Option 1</Menu.Item>
    <Menu.Item padding={false}>Option 2</Menu.Item>
  </Menu>
</SplitButton>`;

export default ({ children }) => (
  <MdxLayout
    title="Split button"
    description="Combine actions and secondary actions within the same element"
    showMeta
    features={[
      "Keyboard accessible",
      "Focusable even when disabled",
      "Multiple styles and sizes",
      "Custom popover and icon",
      "Themes ready",
    ]}
    links={[
      {
        label: "ARIA design",
        url: "https://www.w3.org/TR/wai-aria-1.2/#button",
      },
      {
        label: "ARIA authoring",
        url: "https://www.w3.org/TR/wai-aria-practices-1.2/#button",
      },
    ]}
    hero={
      <Playground
        liveAreaProps={{ direction: "row" }}
        scope={{ ...Components }}
        code={example}
      />
    }
  >
    {children}
  </MdxLayout>
);

## Anatomy

```tsx highlight=1,5-10
import { SplitButton } from "@wonderflow/react-components";

export default () => {
  return (
    <SplitButton label="Button">
      <Menu>
        <Menu.Item>Option 1</Menu.Item>
        <Menu.Item>Option 2</Menu.Item>
      </Menu>
    </SplitButton>
  );
};
```

## API Reference

<PropsTable
  properties={[
    {
      name: "kind",
      type: ['enum'],
      typeValue: "ButtonProps['kind']",
      typeLink: "/components/actions/button#prop-kind",
      default: 'primary',
      required: false,
      description: "Set the style of the button. When the button is `disabled` this property is ignored.",
    },
    {
      name: "dimension",
      type: ['enum'],
      typeValue: "ButtonProps['dimension']",
      typeLink: "/components/actions/button#prop-dimension",
      default: 'regular',
      required: false,
      description: "Set the size of the button.",
    },
    {
      name: "fullWidth",
      type: ["boolean"],
      typeValue: "ButtonProps['fullWidth']",
      typeLink: "/components/actions/button#prop-fullwidth",
      default: "false",
      required: false,
      description: "Make the button full width, filling the available space.",
    },
    {
      name: "icon",
      type: ["enum"],
      typeValue: "ButtonProps['icon']",
      typeLink: "/components/actions/button#prop-icon",
      default: "",
      required: false,
      description: "Define the icon to use",
    },
    {
      name: "disabled",
      type: ["boolean"],
      typeValue: "ButtonProps['disabled']",
      typeLink: "/components/actions/button#prop-disabled",
      default: "",
      required: false,
      description: "Set disabled state. The button is not interactive and grayed out.",
    },
    {
      name: "busy",
      type: ["boolean"],
      typeValue: "ButtonProps['busy']",
      typeLink: "/components/actions/button#prop-busy",
      default: "",
      required: false,
      description: "Set the loading state and show a spinner.",
    },
    {
      name: "onClick",
      type: ["function"],
      typeValue: "ButtonProps['onClick']",
      typeLink: "/components/actions/button#prop-onclick",
      default: "",
      required: false,
      description: "Callback function to be called when the button is pressed.",
    },
    {
      name: "placement",
      type: ["enum"],
      typeValue: "PopoverProps['placement']",
      typeLink: "/components/dialogs/popover#prop-placement",
      default: "auto-start",
      required: false,
      description:
        "The placement of the popover. This is automatically handled based on scroll and viewport edges. By default the popover is anchored at the beginning of the trigger. The first key refers to the X axis, the second key refers to the Y axis. Eg: `auto-start` means the popover will be placed automatically on left or right based on the available space, and anchored at the top (start) of the trigger.",
    },
    {
      name: "offset",
      type: ["number"],
      typeValue: "PopoverProps['offset']",
      typeLink: "/components/dialogs/popover#prop-offset",
      default: "8",
      required: false,
      description: "The distance from the trigger to the popover.",
    },
    {
      name: "as",
      type: ["enum"],
      typeValue: "keyof JSX.IntrinsicElements | React.ComponentType<any>",
      default: 'button',
      required: false,
      description:
        "Set the component to a different HTML tag or custom component",
    }
  ]}
/>

## Accessibility

### Keyboard interactions

<Props
  properties={[
    {
      name: "tab",
      description: "Move focus on the elements or outside",
    },
    {
      name: "enter",
      description: "Trigger the component action",
    },
  ]}
/>
