import { MdxLayout } from "@/components/layouts/mdx";
import { Playground } from "@/components/playground";
import { PropsTable } from "@/components/props-table";
import { Props } from "@/components/props";
import * as components from "@wonderflow/react-components";

export default ({ children }) => (
  <MdxLayout
    title="Toggle button"
    description="When the action a button invoke is so clear that needs no words, just an icon"
    showMeta
    features={[
      "Keyboard accessible",
      "Focusable even when disabled",
      "Multiple styles and sizes",
      "Two icons state",
      "Themes ready",
    ]}
    links={[
      {
        label: "ARIA design",
        url: "https://www.w3.org/TR/wai-aria-1.2/#button",
      },
      {
        label: "ARIA authoring",
        url: "https://www.w3.org/TR/wai-aria-practices-1.2/#button",
      },
    ]}
    hero={
      <Playground
        liveAreaProps={{ direction: "row" }}
        scope={{ ...components }}
        code={`<ToggleButton restingIcon="play" pressedIcon="pause" />`}
      />
    }
  >
    {children}
  </MdxLayout>
);

## Anatomy

```tsx highlight=1,4
import { ToggleButton } from "@wonderflow/react-components";

export default () => {
  return <ToggleButton restingIcon="play" pressedIcon="pause" />;
};
```

## API Reference

<PropsTable
  properties={[
    {
      name: "kind",
      type: ['enum'],
      typeValue: "ButtonProps['kind']",
      typeLink: "/components/actions/button#prop-kind",
      default: 'primary',
      required: false,
      description: "Set the style of the button. When the button is `disabled` this property is ignored.",
    },
    {
      name: "dimension",
      type: ['enum'],
      typeValue: "ButtonProps['dimension']",
      typeLink: "/components/actions/button#prop-dimension",
      default: 'regular',
      required: false,
      description: "Set the size of the button.",
    },
    {
      name: "restingIcon",
      type: ["enum"],
      typeValue: "ButtonProps['icon']",
      typeLink: "/components/actions/button#prop-icon",
      default: "",
      required: false,
      description: "Set the icon to show while not pressed",
    },
    {
      name: "pressedIcon",
      type: ["enum"],
      typeValue: "ButtonProps['icon']",
      typeLink: "/components/actions/button#prop-icon",
      default: "",
      required: false,
      description: "Set the icon to show while not pressed",
    },
    {
      name: "disabled",
      type: ["boolean"],
      typeValue: "ButtonProps['disabled']",
      typeLink: "/components/actions/button#prop-disabled",
      default: "",
      required: false,
      description: "Set disabled state. The button is not interactive and grayed out.",
    },
    {
      name: "pressed",
      type: ["boolean"],
      typeValue: "ButtonProps['pressed']",
      typeLink: "/components/actions/button#prop-pressed",
      default: "false",
      required: false,
      description: "Set the pressed state and add required aria attributes.",
    },
    {
      name: "onClick",
      type: ["function"],
      typeValue: "ButtonProps['onClick']",
      typeLink: "/components/actions/button#prop-onclick",
      default: "",
      required: false,
      description: "Callback function to be called when the button is pressed.",
    },
    {
      name: "as",
      type: ["enum"],
      typeValue: "keyof JSX.IntrinsicElements | React.ComponentType<any>",
      default: 'button',
      required: false,
      description:
        "Set the component to a different HTML tag or custom component",
    }
  ]}
/>

## Accessibility

Adheres to the [`button` role requirements](https://www.w3.org/TR/wai-aria-1.2/#button). This component always requires the `aria-label` or `aria-labelledby` attributes to describe the action.

### Keyboard interactions

<Props
  properties={[
    {
      name: "tab",
      description: "Move focus on the element or outside",
    },
    {
      name: "enter",
      description: "Trigger the component action",
    },
  ]}
/>
