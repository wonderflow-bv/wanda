import { MdxLayout } from "@/components/layouts/mdx";
import { Playground } from "@/components/playground";
import { PropsTable } from "@/components/props-table";
import * as components from "@wonderflow/react-components";

export const example = `<InfoState
  title="Need help?"
  image="https://wonderimages.gumlet.io/placeholders/pyramid.png?format=webp&q=100"
  actions={
    <>
      <Button>Primary</Button>
      <Button kind="flat">Secondary</Button>
    </>
  }
>
  Cras ultricies, elit sit amet cursus consectetur, risus felis ullamcorper
  nulla.
</InfoState>`;

export default ({ children }) => (
  <MdxLayout
    title="Info state"
    description="Provide informations when data is missing"
    showMeta
    features={[
      "Supports icons and images",
      "Multiple directions",
      "Themes ready",
    ]}
    hero={
      <Playground
        liveAreaProps={{ minHeight: "600px" }}
        scope={{ ...components }}
        code={example}
      />
    }
  >
    {children}
  </MdxLayout>
);

## Anatomy

```tsx highlight=1,5-7
import { InfoState } from "@wonderflow/react-components";

export default () => {
  return (
    <InfoState title="Need help?" icon="compass">
      ...
    </InfoState>
  );
};
```

## API Reference

<PropsTable
  properties={[
    {
      name: "title",
      type: ["ReactNode"],
      default: "",
      required: true,
      description: "Set the main tagline of the info state. This should be catchy and short as much as possible.",
    },
    {
      name: "icon",
      type: ["enum"],
      typeValue: "IconProps['source']",
      typeLink: "/components/other/icon#prop-source",
      default: "",
      required: false,
      description: "The icon to display. This is used to enforce the message of the info state. This is not displayed if `image` is set.",
    },
    {
      name: "iconColor",
      type: ["enum"],
      typeValue: "'gray' | 'cyan' | 'green' | 'purple' | 'yellow' | 'red' | 'blue'",
      default: '"blue"',
      required: false,
      description: "Set the icon color. Please use the correct color based on the type of the message. Eg. Don't use `green` for negative informations.",
    },
    {
      name: "image",
      type: ["string"],
      default: "",
      required: false,
      description: "An image may be used instead of an icon. The image is centered and scaled.",
    },
    {
      name: "direction",
      type: ["enum"],
      typeValue: "StackProps['direction']",
      typeLink: "/components/layouts/stack#prop-direction",
      default: "column",
      required: false,
      description: "Set the direction of the content (column or row).",
    },
    {
      name: "actions",
      type: ["ReactNode"],
      default: "",
      required: false,
      description: "Pass the suplementary actions to the info state. Even if you can pass as many elements as you want, we suggest to add no more than two actions.",
    },
  ]}
/>
