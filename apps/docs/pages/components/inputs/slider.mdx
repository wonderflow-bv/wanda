import { MdxLayout } from "@/components/layouts/mdx";
import { Playground } from "@/components/playground";
import { PropsTable } from "@/components/props-table";
import { Props } from "@/components/props";
import * as Components from "@wonderflow/react-components";

export const example = `<Slider aria-label="Items count" showValues />`;

export default ({ children }) => (
  <MdxLayout
    title="Slider"
    description="An input where the user selects a value from within a given range."
    showMeta
    features={[
      "Multiple sizes",
      "Can be controlled or uncontrolled",
      "Supports touch or click on track to update value",
      "Supports Right to Left direction",
      "Full keyboard navigation",
    ]}
    links={[
      {
        label: "ARIA design",
        url: "https://www.w3.org/TR/wai-aria-1.2/#slider",
      },
      {
        label: "ARIA authoring",
        url: "https://www.w3.org/TR/wai-aria-practices-1.2/#slider",
      },
    ]}
    hero={<Playground scope={{ ...Components }} code={example} />}
  >
    {children}
  </MdxLayout>
);

## Anatomy

```tsx highlight=1,4
import { Slider } from "@wonderflow/react-components";

export default () => {
  return <Slider />;
};
```

## Examples

### With icons

If you don't want to use numeric values you can use icons instead.

export const exampleWithIcons = `<Slider aria-label="brightness" iconMin="moon" iconMax="sun-bright" />`;

<Playground scope={{ ...Components }} code={exampleWithIcons} />

## API Reference

<PropsTable
  properties={[
    {
      name: "min",
      type: ["number"],
      default: "0",
      required: false,
      description: "Set the minimum value of the slider. This value must be lower than `max`.",
    },
    {
      name: "max",
      type: ["number"],
      default: "100",
      required: false,
      description: "Set the maximum value of the slider. This value must be higher than `min`.",
    },
    {
      name: "step",
      type: ["number"],
      default: "",
      required: false,
      description: "Set the step value of the slider. This allows the user to select a value in increments of `step`.",
    },
    {
      name: "onInput",
      type: ["function"],
      typeValue: "(value: number) => void",
      default: "",
      required: false,
      description:
        "Callback function to be called when the value is changed. A parameter `number` is passed to get the new value.",
    },
    {
      name: "defaultValue",
      type: ["number"],
      default: "0",
      required: false,
      description: "Set the initial value of the slider.",
    },
    {
      name: "showValues",
      type: ["boolean"],
      default: "",
      required: false,
      description: "Show or hide the values beside the slider.",
    },
    {
      name: "iconMin",
      type: ["enum"],
      typeValue: "IconProps['source']",
      typeLink: "/components/other/icon#prop-source",
      default: "",
      required: false,
      description: "Add an icon representing the minimum value. This is only available when `showValues` is `false`.",
    },
    {
      name: "iconMax",
      type: ["enum"],
      typeValue: "IconProps['source']",
      typeLink: "/components/other/icon#prop-source",
      default: "",
      required: false,
      description: "Add an icon representing the maximum value. This is only available when `showValues` is `false`",
    },
    {
      name: "dimension",
      type: ['enum'],
      typeValue: "'small' | 'regular'",
      default: 'regular',
      required: false,
      dangerous: false,
      description: "Set the element size",
    },
  ]}
/>

## Accessibility

Adheres to the [`slider` role requirements](https://www.w3.org/TR/wai-aria-1.2/#slider).

### Keyboard interactions

<Props
  properties={[
    {
      name: "ArrowRight",
      description:
        "Increments/decrements by the step value depending on orientation",
    },
    {
      name: "ArrowLeft",
      description:
        "Increments/decrements by the step value depending on orientation",
    },
    {
      name: "ArrowUp",
      description: "Increases the value by the step amount",
    },
    {
      name: "ArrowDown",
      description: "IDecreases the value by the step amount",
    },
    {
      name: "PageUp",
      description: "Increases the value by a larger step",
    },
    {
      name: "PageDown",
      description: "Decreases the value by a larger step",
    },
    {
      name: "Home",
      description: "Sets the value to its minimum",
    },
    {
      name: "End",
      description: "Sets the value to its maximum",
    },
  ]}
/>
