import { MdxLayout } from "@/components/layouts/mdx";
import { Playground } from "@/components/playground";
import { PropsTable } from "@/components/props-table";
import { Props } from "@/components/props";
import * as components from "@wonderflow/react-components";

export const example = `<Textfield
  type="text"
  label="Label"
  defaultValue="Prefilled value"
/>`;

export default ({ children }) => (
  <MdxLayout
    title="Textfield"
    description="Text inputs enable the user to interact with and input content and data."
    showMeta
    features={[
      "Keyboard accessible",
      "Can contain icons",
      "Multiple sizes",
      "Value format validation",
      "Can be rendered as textarea",
      "Themes ready",
    ]}
    links={[
      {
        label: "ARIA design",
        url: "https://www.w3.org/TR/wai-aria-1.2/#button",
      },
      {
        label: "ARIA authoring",
        url: "https://www.w3.org/TR/wai-aria-practices-1.2/#button",
      },
    ]}
    hero={
      <Playground
        liveAreaProps={{ direction: "row" }}
        scope={{ ...components }}
        code={example}
      />
    }
  >
    {children}
  </MdxLayout>
);

## Anatomy

```tsx highlight=1,5-10
import { Textfield } from "@wonderflow/react-components";

export default () => {
  return (
    <Textfield type="text" label="Filled" defaultValue="Prefilled value" />
  );
};
```

## API Reference

<PropsTable
  properties={[
    {
      name: "icon",
      type: ["enum"],
      typeValue: "IconProps['source']",
      typeLink: "/components/other/icon#prop-source",
      default: "",
      required: false,
      description: "Define the icon to use",
    },
    {
      name: "iconPosition",
      type: ['enum'],
      typeValue: "'left' | 'right'",
      default: 'right',
      required: false,
      description: "Set in which side of the field the icon should be displayed.",
    },
    {
      name: "label",
      type: ["string"],
      default: "",
      required: false,
      description: "Define the accessible label of the input. While this is not mandatory, an input should always have a label. If not using this property you can bind a custom label to the input by using an id.",
    },
    {
      name: "textarea",
      type: ["boolean"],
      default: "",
      required: false,
      description: "Set the input to be a textarea instead of a single line field. This property completely changes the rendered element from an input to a textarea.",
    },
    {
      name: "dimension",
      type: ['enum'],
      typeValue: "'regular' | 'small' | 'big'",
      default: 'regular',
      required: false,
      description: "Set the size of the field",
    },
    {
      name: "readOnly",
      type: ["boolean"],
      default: "",
      required: false,
      description: "Set the field into a readonly state. When readonly, the field value cannot be edited but it can still be selected and copied.",
    },
    {
      name: "disabled",
      type: ["boolean"],
      default: "false",
      required: false,
      description: "Set the field into a disabled state. When disabled, the field value cannot be edited, selected or copied, but it can still be focused and navigated by AT.",
    },
    {
      name: "invalid",
      type: ["boolean"],
      default: "",
      required: false,
      description: "Set the invalid state",
    },
    {
      name: "onChange",
      type: ["function"],
      typeValue: "(ChangeEvent<HTMLInputElement | HTMLTextAreaElement>) => void",
      default: "",
      required: false,
      description: "The callback function that is called when the input value changes.",
    },
  ]}
/>

## Accessibility

The `Textfield` component requires an associated `label` element and `id` to describes the field. You can use the `label` and `id` properties or add a custom external `label`.

### Keyboard interactions

<Props
  properties={[
    {
      name: "tab",
      description: "Move focus on the element or outside",
    },
  ]}
/>
