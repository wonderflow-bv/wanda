import { MdxLayout } from "@/components/layouts/mdx";
import { Playground } from "@/components/playground";
import { PropsTable } from "@/components/props-table";
import * as components from "@wonderflow/react-components";
import tkns from "@wonderflow/tokens/platforms/web/tokens.json";

export const example = `<Card
  left={<Icon source="message" dimension={32} />}
  right={<Icon source="compass" dimension={32} />}
  vibrant
>
  Lorem ipsum dolor sit, amet consectetur adipisicing elit. Eius
  dolores, tempore quas labore officiis praesentium. Porro sed dolorem,
  numquam temporibus.
</Card>`;

export default ({ children }) => (
  <MdxLayout
    title="Card"
    description="Wrap your content with style"
    showMeta
    features={["Highly customizable", "Multiple slots", "Polymorphic"]}
    hero={<Playground scope={{ ...components }} code={example} />}
  >
    {children}
  </MdxLayout>
);

## Anatomy

```tsx highlight=1,4
import { Card } from "@wonderflow/react-components";

export default () => {
  return <Card>Lorem ipsum dolor sit.</Card>;
};
```

## API Reference

<PropsTable
  properties={[
    {
      name: "wrap",
      type: ["boolean"],
      typeValue: "StackProps['wrap']",
      typeLink: "/components/layouts/stack#prop-wrap",
      default: "false",
      required: false,
      description: "Wrap `left`, `children` and `right` there is no space for them",
    },
    {
      name: "columnGap",
      type: "enum",
      typeValue: "StackProps['columnGap']",
      typeLink: "/components/layouts/stack#prop-columngap",
      default: "24",
      required: false,
      description: "Set the space between `left`, `children` and `right`",
    },
    {
      name: "verticalAlign",
      type: ["string"],
      typeValue: "StackProps['verticalAlign']",
      typeLink: "/components/layouts/stack#prop-verticalalign",
      default: "start",
      required: false,
      description:
        "Set the vertical content placement instead of growing to fill the available space.",
    },
    {
      name: "horizontalAlign",
      type: ["string"],
      typeValue: "StackProps['horizontalAlign']",
      typeLink: "/components/layouts/stack#prop-horizontalalign",
      default: "space-between",
      required: false,
      description:
        "Set the horizontal content placement instead of growing to fill the available space.",
    },
    {
      name: "padding",
      type: ["enum"],
      typeValue: `false | ${Object.keys(tkns.space).map(name => `${name}`).join(" | ")}`,
      default: "24",
      required: false,
      dangerous: false,
      description: "Set the padding or remove it",
    },
    {
      name: "left",
      type: ["ReactNode"],
      default: "",
      required: false,
      description: "ontent rendered inside the card on the left side, before children.",
    },
    {
      name: "right",
      type: ["ReactNode"],
      default: "",
      required: false,
      description: "Content rendered inside the card on the right side, after children.",
    },
    {
      name: "bordered",
      type: ["boolean"],
      default: "",
      required: false,
      description: "Add a border to the card to increase its visual weight and contrast.",
    },
    {
      name: "radius",
      type: ["enum"],
      typeValue: `false | ${Object.keys(tkns.radius).map(name => `${name}`).join(" | ")}`,
      default: "8",
      required: false,
      description: "Define the edge radius of the card.",
    },
    {
      name: "dimmed",
      type: ["enum"],
      typeValue: "1 | 2 | 3",
      default: "",
      required: false,
      description: "Change the background color of the card.",
    },
    {
      name: "vibrant",
      type: ["boolean"],
      default: "",
      required: false,
      description: "Make the card vibrant. Add tranlucent background.",
    },
    {
      name: "highlightOnHover",
      type: ["boolean"],
      default: "",
      required: false,
      description:
        "Change the background color of the card when it is hovered.",
    },
    {
      name: "as",
      type: ["enum"],
      typeValue: "keyof JSX.IntrinsicElements | React.ComponentType<any>",
      default: 'div',
      required: false,
      description:
        "Set the component to a different HTML tag or custom component",
    }
  ]}
/>
