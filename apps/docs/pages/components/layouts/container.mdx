import { MdxLayout } from "@/components/layouts/mdx";
import { Playground } from "@/components/playground";
import { PropsTable } from "@/components/props-table";
import * as components from "@wonderflow/react-components";

export const example = `<Container dimension="large" className="ContainerEx"></Container>`;

export default ({ children }) => (
  <MdxLayout
    title="Container"
    description="Who contains the content"
    showMeta
    features={[
      "Auto centering",
      "Predefined sizes",
      "Auto adjust paddings",
      "Fluid by default",
    ]}
    hero={
      <Playground
        liveAreaProps={{ fill: true }}
        scope={{ ...components }}
        code={example}
      />
    }
  >
    {children}
  </MdxLayout>
);

## Anatomy

```tsx highlight=1,4
import { Container } from "@wonderflow/react-components";

export default () => {
  return <Container>...</Container>;
};
```

## API Reference

<PropsTable
  properties={[
    {
      name: "dimension",
      type: ['enum'],
      typeValue: "'full' | 'small' | 'medium' | 'large'",
      default: 'full',
      required: false,
      description: "Set the horizontal max-width of the container.",
    },
    {
      name: "padding",
      type: ["boolean"],
      default: "true",
      required: false,
      description: "Set or remove the container's predefined horizontal padding.",
    },
    {
      name: "as",
      type: ["enum"],
      typeValue: "keyof JSX.IntrinsicElements | React.ComponentType<any>",
      default: 'div',
      required: false,
      description:
        "Set the component to a different HTML tag or custom component",
    }
  ]}
/>
