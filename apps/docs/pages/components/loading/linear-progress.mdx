import { MdxLayout } from "@/components/layouts/mdx";
import { Playground } from "@/components/playground";
import { PropsTable } from "@/components/props-table";
import * as components from "@wonderflow/react-components";

export const example = `<LinearProgress showProgress value={30} />`;

export default ({ children }) => (
  <MdxLayout
    title="Linear progress"
    description="Indicates determinate and indeterminate loading status"
    showMeta
    features={[
      "Multiple sizes",
      "Supports determinate and indeterminate states",
      "Themes ready",
    ]}
    hero={
      <Playground
        liveAreaProps={{ fill: true }}
        scope={{ ...components }}
        code={example}
      />
    }
    links={[
      {
        label: "ARIA design",
        url: "https://www.w3.org/TR/wai-aria-1.2/#progressbar",
      },
    ]}
  >
    {children}
  </MdxLayout>
);

## Anatomy

```tsx highlight=1,4
import { LinearProgress } from "@wonderflow/react-components";

export default () => {
  return <LinearProgress />;
};
```

## API Reference

<PropsTable
  properties={[
    {
      name: "value",
      type: ["number"],
      default: "",
      required: false,
      description:
        "Set the current progress of the progress bar. This value should be between 0 and 'max'. The percentage is automatically computed.",
    },
    {
      name: "max",
      type: ["number"],
      default: "100",
      required: false,
      description: "Set the max value of the progress bar. This determines the computed percentage.",
    },
    {
      name: "dimension",
      type: ['enum'],
      typeValue: "'regular' | 'big'",
      default: 'regular',
      required: false,
      description: "Set the dimension of the progress bar.",
    },
    {
      name: "showProgress",
      type: ["boolean"],
      default: "",
      required: false,
      description: "Show or hide the progress value.",
    },
  ]}
/>

## Accessibility

Adheres to the [`progressbar` role requirements](https://www.w3.org/TR/wai-aria-1.2/#progressbar).
