import { MdxLayout } from "@/components/layouts/mdx";
import { Playground } from "@/components/playground";
import { PropsTable } from "@/components/props-table";
import { Props } from "@/components/props";
import * as Components from "@wonderflow/react-components";

export const example = `<Pagination
  itemsCount={1000}
  itemsPerPage={10}
  initialPage={18}
  onPageClick={({selected, offset}) => console.log(selected, offset)}
  disableInitialCallback={true}
/>`;

export default ({ children }) => (
  <MdxLayout
    title="Pagination"
    description="Pagination helps users navigate between large groups of content separated by pages."
    showMeta
    features={[
      "Keyboard accessible",
      "Dynamic ranges",
      "Can be controlled or uncontrolled",
      "Themes ready",
    ]}
    hero={<Playground scope={{ ...Components }} code={example} />}
  >
    {children}
  </MdxLayout>
);

## Anatomy

```tsx highlight=1,4
import { Pagination } from "@wonderflow/react-components";

export default () => {
  return <Pagination itemsCount={1000} />;
};
```

## API Reference

This component extends the [`react-paginate`](https://github.com/AdeleD/react-paginate) library, which exposes different props that enables multiple functionalities.

<Components.Button
  kind="secondary"
  as="a"
  href="https://github.com/AdeleD/react-paginate#props"
  target="_blank"
  rel="noopener noreferrer"
>
  Original API reference
</Components.Button>

### Custom extended API

<PropsTable
  properties={[
    {
      name: "itemsCount",
      type: ["number"],
      default: "",
      required: true,
      description: "Set the total number of items to paginate through.",
    },
    {
      name: "itemsPerPage",
      type: ["number"],
      default: "10",
      required: false,
      description: "Set the number of items to display per page.",
    },
    {
      name: "pageCount",
      type: ["number"],
      default: "",
      required: false,
      description:
        "Set the number of pages to display. If missing this is computed by the `itemsCount` divided by `itemsPerPage`.",
    },
    {
      name: "onPageClick",
      type: ["function"],
      typeValue: "(offset: Record<string, number>) => void",
      default: "",
      required: false,
      description: "Callback function to be called when the page is changed. A an `object` is passed with the following properties:\n - `selected`: The index of the selected page.\n - `offset`: The offset of the selected page.",
    },
    {
      name: "pageRangeDisplayed",
      type: ["number"],
      default: "3",
      required: false,
      description: "Set how many pages to show in the visible page range (between the '...' break)",
    },
    {
      name: "marginPagesDisplayed",
      type: ["number"],
      default: "3",
      required: false,
      dangerous:false,
      description: "The number of visible pages to display on the sides.",
    },
  ]}
/>

## Accessibility

### Keyboard interactions

<Props
  properties={[
    {
      name: "tab",
      description: "When focus moves onto the next element of the pagination",
    },
    {
      name: "Enter",
      description: "Activate the focused page",
    },
  ]}
/>
