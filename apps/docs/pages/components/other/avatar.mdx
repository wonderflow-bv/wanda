import { MdxLayout } from "@/components/layouts/mdx";
import { Playground } from "@/components/playground";
import { PropsTable } from "@/components/props-table";
import * as components from "@wonderflow/react-components";

export const example = `<>
  <Avatar src="https://api.lorem.space/image/face?w=150&h=150" />
  <Avatar />
</>`;

export default ({ children }) => (
  <MdxLayout
    title="Avatar"
    description="Help user identify person"
    showMeta
    features={[
      "Multiple sizes",
      "Automatic fallback if image is missing",
      "Themes ready",
    ]}
    hero={<Playground scope={{ ...components }} code={example} />}
  >
    {children}
  </MdxLayout>
);

## Anatomy

```tsx highlight=1,4
import { Avatar } from "@wonderflow/react-components";

export default () => {
  return <Avatar src="..." />;
};
```

## API Reference

<PropsTable
  properties={[
    {
      name: "src",
      type: ["string"],
      default: "",
      required: true,
      description: "The source of the image to use as avatar",
    },
    {
      name: "dimension",
      type: ['enum'],
      typeValue: "'small' | 'regular' | 'big'",
      default: 'regular',
      required: false,
      dangerous: false,
      description: "Define the size of the avatar",
    },
  ]}
/>
