import { MdxLayout } from "@/components/layouts/mdx";
import { Playground } from "@/components/playground";
import { PropsTable } from "@/components/props-table";
import { Props } from "@/components/props";
import * as components from "@wonderflow/react-components";

export const example = `<Chip color="blue" interactive>Chip text</Chip>`;

export default ({ children }) => (
  <MdxLayout
    title="Chip"
    description="Dismissable tag elements"
    showMeta
    features={[
      "Keyboard accessible",
      "Multiple styles and sizes",
      "Dismissable",
      "Themes ready",
    ]}
    hero={<Playground scope={{ ...components }} code={example} />}
  >
    {children}
  </MdxLayout>
);

## Anatomy

```tsx highlight=1,4
import { Chip } from "@wonderflow/react-components";

export default () => {
  return <Chip>Text</Chip>;
};
```

## API Reference

<PropsTable
  properties={[
    {
      name: "dimension",
      type: ['enum'],
      typeValue: "'small' | 'regular' | 'big'",
      default: 'regular',
      required: false,
      description: "Set the dimension of the component.",
    },
    {
      name: "color",
      type: ["enum"],
      typeValue: "'gray' | 'cyan' | 'green' | 'purple' | 'yellow' | 'red' | 'blue'",
      default: 'gray',
      required: false,
      description: "Set a color from one of the provided values.",
    },
    {
      name: "interactive",
      type: ["boolean"],
      default: "",
      required: false,
      description: "Make the chip dismissable. When `true` adds a close button on the side.",
    },
    {
      name: "onDismissClick",
      type: ["function"],
      typeValue: "() => void",
      default: "",
      required: false,
      description: "Callback function to be called when the dismiss button is pressed.",
    },
  ]}
/>

## Accessibility

### Keyboard interactions

<Props
  properties={[
    {
      name: "tab",
      description: "If interactive, move focus on the button element inside",
    },
  ]}
/>
