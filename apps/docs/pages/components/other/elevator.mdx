import { MdxLayout } from "@/components/layouts/mdx";
import { Playground } from "@/components/playground";
import { PropsTable } from "@/components/props-table";
import * as components from "@wonderflow/react-components";

export const example = `<Elevator resting={1} hover={4}>
  <Card>
    Lorem ipsum dolor sit, amet consectetur adipisicing elit. Eius
    dolores, tempore quas labore officiis praesentium. Porro sed
    dolorem, numquam temporibus.
  </Card>
</Elevator>`;

export default ({ children }) => (
  <MdxLayout
    title="Elevator"
    description="Elevate you elements across the z axis"
    showMeta
    features={[
      "It doesn't generate any wrapper",
      "Follows Wanda elevations styles",
      "Elevation can change on hover",
    ]}
    hero={<Playground scope={{ ...components }} code={example} />}
  >
    {children}
  </MdxLayout>
);

## Anatomy

```tsx highlight=1,5-7
import { Elevator, Card } from "@wonderflow/react-components";

export default () => {
  return (
    <Elevator resting={1} hover={4}>
      <Card>Lorem ipsum dolor sit.</Card>
    </Elevator>
  );
};
```

## API Reference

<PropsTable
  properties={[
    {
      name: "resting",
      type: ["enum"],
      typeValue: "0 | 1 | 2 | 3 | 4",
      default: "",
      required: true,
      description: "Set the elevation of the component when is resting.",
    },
    {
      name: "hover",
      type: ["enum"],
      typeValue: "0 | 1 | 2 | 3 | 4",
      default: "",
      required: false,
      description: "Set the elevation of the component when is hovered.",
    },
    {
      name: "children",
      type: ["ReactElement"],
      default: "",
      required: true,
      description: "The content to be elevated. This component doesn't generate any wrapper. It just applies the elevation by adding the respective attribvtes to the children.",
    },
  ]}
/>
