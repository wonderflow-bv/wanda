import { MdxLayout } from "@/components/layouts/mdx";
import { Playground } from "@/components/playground";
import { PropsTable } from "@/components/props-table";
import * as components from "@wonderflow/react-components";

export default ({ children }) => (
  <MdxLayout
    title="Star meter"
    description="Visual representation of a star rating."
    showMeta
    features={[
      "Dynamic rounding",
      "Custom label",
      "Custom max value",
      "Multiple sizes",
      "Themes ready",
    ]}
    links={[
      {
        label: "ARIA design",
        url: "https://www.w3.org/TR/wai-aria-1.2/#meter",
      },
      {
        label: "ARIA authoring",
        url: "https://www.w3.org/TR/wai-aria-practices-1.2/#meter",
      },
    ]}
    hero={
      <Playground
        scope={{ ...components }}
        code={`<StarMeter value={3.58} />`}
      />
    }
  >
    {children}
  </MdxLayout>
);

## Anatomy

```tsx highlight=1,4
import { StarMeter } from "@wonderflow/react-components";

export default () => {
  return <StarMeter value={3.58}>;
};
```

## API Reference

<PropsTable
  properties={[
    {
      name: "value",
      type: ["number"],
      default: "0",
      required: true,
      description: "Set the value of the star meter. This value must be between `0` and `starCount`. This data is used to calculate the number of filled stars based on predefined thresholds of the fraction. \n\n```bash\nfraction < `0.25` = rounded to 0 (empty star) \nfraction >= `0.75` = rounded to 1 (filled star) \nfraction >= `0.25` and < 0.75 = rounded to 0.5 (half star)\n```",
    },
    {
      name: "starCount",
      type: ["number"],
      default: "5",
      required: false,
      description: "Set the number of stars to use as maximum.",
    },
    {
      name: "label",
      type: ["ReactNode"],
      default: "",
      required: false,
      description: "Set a custom label instead of the current value.",
    },
    {
      name: "dimension",
      type: ['enum'],
      typeValue: "'small' | 'regular' | 'big'",
      default: '"regular"',
      required: false,
      description: "Set the size of the star meter.",
    },
    {
      name: "hideLabel",
      type: ["boolean"],
      default: "false",
      required: false,
      description: "Hide the label beside the stars",
    },
  ]}
/>

## Accessibility

Adheres to the [`meter` role requirements](https://www.w3.org/TR/wai-aria-1.2/#meter).
