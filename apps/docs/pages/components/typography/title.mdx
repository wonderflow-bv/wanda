import { MdxLayout } from "@/components/layouts/mdx";
import { Playground } from "@/components/playground";
import { PropsTable } from "@/components/props-table";
import * as components from "@wonderflow/react-components";

export const example = `<Title as="h3" level="3">Title</Title>`;

export default ({ children }) => (
  <MdxLayout
    title="Title"
    description="Dynamic and responsive titles with control"
    showMeta
    features={[
      "Dynamic HTML tag",
      "Fully customizzable",
      "Responsive text",
      "Decoupled size and semantic",
      "Themes ready",
    ]}
    hero={<Playground scope={{ ...components }} code={example} />}
  >
    {children}
  </MdxLayout>
);

## Anatomy

```tsx highlight=1,4
import { Title } from "@wonderflow/react-components";

export default () => {
  return <Title>Title</Title>;
};
```

## API Reference

<PropsTable
  properties={[
    {
      name: "level",
      type: ['enum'],
      typeValue: "'1' | '2' | '3' | '4' | '5' | '6' | 'display'",
      default: '1',
      required: false,
      description: "Set the level of the title. This property only affects the visual appearance of the title and not the semantic meaning of the title, which you can define by using the `as` property.",
    },
    {
      name: "lineHeight",
      type: ['enum'],
      typeValue: "'none' | 'small' | 'large'",
      default: 'small',
      required: false,
      description: "Set the text line-height of the title. This uses the predefined tokens from the typography system.",
    },
    {
      name: "textAlign",
      type: ['enum'],
      typeValue: "'start' | 'center' | 'end'",
      default: "start",
      required: false,
      description:
        "Set the text alignment of the title based on the text direction.",
    },
    {
      name: "maxWidth",
      type: ["string"],
      default: "",
      required: false,
      description:
        "Set the maximum width of the text after which it will wrap.",
    },
    {
      name: "responsive",
      type: ["boolean"],
      default: "true",
      required: false,
      description:
        "Enable or disable the responsiveness of the text. If disabled, the text will be always the same size across all breakpoints.",
    },
    {
      name: "as",
      type: ["enum"],
      typeValue: "keyof JSX.IntrinsicElements | React.ComponentType<any>",
      default: 'span',
      required: false,
      description:
        "Set the component to a different HTML tag or custom component",
    }
  ]}
/>

## Accessibility

The `Title` component is rendered as `span` by default. Make sure to define the correct heading tag when semantic is required to make the content navigable and accessible with AT.

Read more about how to [manage headings and accessibility](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/Heading_Elements#accessibility_concerns).
