import ComponentsNav from "@/data/components-nav";
import { getLayoutProps } from "@/utils/get-layout-props";
import { PropsTable } from "@/components/doc/props-table";
import { KeyTable } from "@/components/shared/key-table";
import { Autocomplete } from "@wonderflow/react-components";

export const getStaticProps = () =>
  getLayoutProps({
    layout: "component",
    layoutProps: {
      title: "Select",
      color: "indigo",
      subtitle: "The simplest way to pick an option from the list",
      navigation: ComponentsNav,
      features: [
        "Keyboard accessible",
        "Customizable icon",
        "Multiple styles and sizes",
        "Themes ready",
      ],
      links: [
        {
          label: "ARIA design",
          url: "https://www.w3.org/TR/wai-aria-1.2/#select",
        },
        {
          label: "ARIA authoring",
          url: "https://www.w3.org/WAI/ARIA/apg/patterns/listbox/",
        },
      ]
    }
  });

<LiveArea direction="row">
  <Select defaultValue="placeholder" style={{ maxInlineSize: "30ch" }}>
    <option value="placeholder" hidden disabled>Pick an option</option>

    <optgroup label="Option Group One">
      <option value="1">This is a very long option</option>
      <option value="2">Option 2</option>
    </optgroup>

    <optgroup label="Option Group Two">
      <option value="3">Option 3</option>
      <option value="4">Option 4</option>
      <option value="5">Option 5</option>
    </optgroup>
  </Select>
</LiveArea>


## Anatomy

```tsx lines="1,5-16"
import { Select } from "@wonderflow/react-components";

export default () => {
  return (
    <Select defaultValue="placeholder">
      <option value="placeholder" hidden disabled>
        Pick an option
      </option>
      <option value="1">Option 1</option>
      <option value="2">Option 2</option>
      <optgroup label="Option Group Two">
        <option value="3">Option 3</option>
        <option value="4">Option 4</option>
        <option value="5">Option 5</option>
      </optgroup>
    </Select>
  );
};
```

## API Reference

<PropsTable
  properties={[
    {
      name: "icon",
      type: ["enum"],
      typeValue: "IconProps['source']",
      typeLink: "/components/other/icon#prop-source",
      default: "sort-alt",
      required: false,
      description: "Change the default icon displayed on the side of the select.",
    },
    {
      name: "label",
      type: ["ReactNode"],
      default: "",
      required: false,
      description: "Set the accessible label for the select.",
    },
    {
      name: "kind",
      type: ['enum'],
      typeValue: "'single' | 'multiple'",
      default: 'single',
      required: false,
      description: "Set how many options can be selected at once.",
    },
    {
      name: "dimension",
      type: ['enum'],
      typeValue: "'regular' | 'small' | 'big'",
      default: 'regular',
      required: false,
      description: "Set the size of the select. This affects alsosize and style of the icon.",
    },
    {
      name: "disabled",
      type: ["boolean"],
      default: "",
      required: false,
      description: "Set disabled state. The select is not interactive and grayed out.",
    },
    {
      name: "onChange",
      type: ["function"],
      typeValue: "(event: ChangeEvent<HTMLSelectElement>) => void",
      default: "",
      required: false,
      description: "Callback function to be called when a new value is selected.",
    },
  ]}
/>

## Accessibility

Adheres to the [`select` role requirements](https://www.w3.org/TR/wai-aria-1.2/#select).

### Keyboard interactions

<KeyTable
  data={[
    {
      name: "tab",
      description: "Move focus on the element or outside",
    },
    {
      name: "space",
      description: "Open the listbox",
    },
  ]}
/>
