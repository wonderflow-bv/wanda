import ComponentsNav from "@/data/components-nav";
import { getLayoutProps } from "@/utils/get-layout-props";
import { PropsTable } from "@/components/doc/props-table";
import tkns from "@wonderflow/tokens/platforms/web/tokens.json";

export const getStaticProps = () =>
  getLayoutProps({
    layout: "component",
    layoutProps: {
      title: "Aspect ratio",
      color: "indigo",
      subtitle: "Displays content within a desired ratio.",
      navigation: ComponentsNav,
      features: [
        "Accepts any CSS custom ratio.",
      ]
    }
  });

<LiveArea>
  <AspectRatio ratio="16/9">
    <Card style={{ width: 400 }}>Content</Card>
  </AspectRatio>
</LiveArea>

## Anatomy

```tsx lines="1,5-7"
import { AspectRatio } from "@wonderflow/react-components";

export default () => {
  return (
    <AspectRatio ratio="1 / 1">
      <div>...</div>
    </AspectRatio>
  );
};
```

## API Reference

<PropsTable
  properties={[
    {
      name: "ratio",
      type: ["string"],
      default: "",
      required: true,
      description: "Set the ratio following the CSS syntax",
    }
  ]}
/>

