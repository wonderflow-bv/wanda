import ComponentsNav from "@/data/components-nav";
import { getLayoutProps } from "@/utils/get-layout-props";
import { PropsTable } from "@/components/doc/props-table";
import { KeyTable } from "@/components/shared/key-table";
import { useState } from "react";
import dynamic from "next/dynamic";

export const DynOverlayContainer = dynamic(
  import("@wonderflow/react-components").then((m) => m.OverlayContainer),
  { ssr: false }
);

export const getStaticProps = () =>
  getLayoutProps({
    layout: "component",
    layoutProps: {
      newLayout: true,
      title: "Button",
      color: "indigo",
      subtitle: "A button is a box area or text that communicates and triggers user actions when clicked.",
      navigation: ComponentsNav,
      features: [
        "Keyboard accessible",
        "Customizable icon",
        "Multiple styles and sizes",
        "Themes ready",
      ],
      links: [
        {
          label: "ARIA design",
          url: "https://www.w3.org/TR/wai-aria-1.2/#button",
        },
        {
          label: "ARIA authoring",
          url: "https://www.w3.org/WAI/ARIA/apg/patterns/button/",
        },
      ]
    }
  });

<LiveArea direction="row">
  <Button icon="check">Button</Button>
</LiveArea>


## Anatomy

```tsx lines="1,4"
import { Button } from "@wonderflow/react-components";

export default () => {
  return <Button>Click</Button>;
};
```

## Grouping buttons

Sometimes you need to group buttons together to create a single visual context or to create a set of related actions. To group buttons, you can use the `ButtonsGroup` component.


<LiveArea>
  <ButtonsGroup dimension="big" kind="secondary">
    <Button>Action 1</Button>
    <Button>Action 2</Button>
    <Button>Action 3</Button>
  </ButtonsGroup>
</LiveArea>


## API Reference

### Button

<PropsTable
  properties={[
    {
      name: "kind",
      type: ['enum'],
      typeValue: "'primary' | 'secondary' | 'flat'",
      default: 'primary',
      required: false,
      description: "Set the style of the button. When the button is `disabled` this property is ignored.",
    },
    {
      name: "dimension",
      type: ['enum'],
      typeValue: "'small' | 'regular' | 'big'",
      default: 'regular',
      required: false,
      description: "Set the size of the button.",
    },
    {
      name: "fullWidth",
      type: ["boolean"],
      default: "false",
      required: false,
      description: "Make the button full width, filling the available space.",
    },
    {
      name: "icon",
      type: ["enum"],
      typeValue: "IconProps['source']",
      typeLink: "/components/other/icon#prop-source",
      default: "",
      required: false,
      description: "Define the icon to use",
    },
    {
      name: "iconPosition",
      type: ['enum'],
      typeValue: "'left' | 'right'",
      default: 'left',
      required: false,
      description: "Set the icon position",
    },
    {
      name: "iconColor",
      type: ["string"],
      default: "",
      required: false,
      description: "Override the color of the icon. Used only when icon is defined.",
    },
    {
      name: "disabled",
      type: ["boolean"],
      default: "",
      required: false,
      description: "Set disabled state. The button is not interactive and grayed out.",
    },
    {
      name: "busy",
      type: ["boolean"],
      default: "",
      required: false,
      description: "Set the loading state and show a spinner.",
    },
    {
      name: "pressed",
      type: ["boolean"],
      default: "false",
      required: false,
      description: "Set the pressed state and add required aria attributes.",
    },
    {
      name: "onClick",
      type: ["function"],
      typeValue: `(MouseEvent<HTMLButtonElement | HTMLAnchorElement>) => void`,
      default: "",
      required: false,
      description: "Callback function to be called when the button is pressed.",
    },
    {
      name: "as",
      type: ["enum"],
      typeValue: "keyof JSX.IntrinsicElements | React.ComponentType<any>",
      default: 'button',
      required: false,
      description:
        "Set the component to a different HTML tag or custom component",
    }
  ]}
/>

### ButtonsGroup

<PropsTable
  properties={[
    {
      name: "kind",
      type: ['enum'],
      typeValue: "'primary' | 'secondary' | 'flat'",
      default: 'primary',
      required: false,
      description: "Set the style of the button. When the button is `disabled` this property is ignored.",
    },
    {
      name: "dimension",
      type: ['enum'],
      typeValue: "'small' | 'regular' | 'big'",
      default: 'regular',
      required: false,
      description: "Set the size of the button.",
    },
  ]}
/>

## Accessibility

Adheres to the [`button` role requirements](https://www.w3.org/TR/wai-aria-1.2/#button).

### Keyboard interactions

<KeyTable
  data={[
    {
      name: "tab",
      description: "Move focus on the element or outside",
    },
    {
      name: "enter",
      description: "Trigger the component action",
    },
    {
      name: "space",
      description: "Trigger the component action",
    },
  ]}
/>
