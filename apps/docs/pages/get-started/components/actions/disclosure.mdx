import ComponentsNav from "@/data/components-nav";
import { getLayoutProps } from "@/utils/get-layout-props";
import { PropsTable } from "@/components/doc/props-table";
import { KeyTable } from "@/components/shared/key-table";

export const getStaticProps = () =>
  getLayoutProps({
    layout: "component",
    layoutProps: {
      title: "Disclosure",
      color: "indigo",
      subtitle: "Interactive element to control the visiblity of content.",
      navigation: ComponentsNav,
      features: [
        "Keyboard accessible",
        "Customizable icon position",
        "Multiple sizes",
        "Themes ready",
      ],
      links: [
        {
          label: "ARIA authoring",
          url: "https://www.w3.org/WAI/ARIA/apg/patterns/disclosure/",
        },
      ]
    }
  });

<LiveArea fill={false} hAlign="start" vAlign="start" minHeight="220px">
  <Disclosure summary="Click to expand">
    <Text maxWidth="60ch" dimmed={6}>Cow ricotta hard cheese. Port-salut dolcelatte pecorino who moved my
      cheese cheddar feta mozzarella camembert de normandie. Fondue fondue
      cheese triangles cauliflower cheese lancashire edam croque monsieur
      blue castello. Pecorino danish fontina ricotta cheddar emmental
      pepper jack.</Text>
  </Disclosure>
</LiveArea>


## Anatomy

```tsx lines="1,4"
import { Disclosure } from "@wonderflow/react-components";

export default () => {
  return <Disclosure summary="Click to expand">...</Disclosure>;
};
```

## API Reference

<PropsTable
  properties={[
    {
      name: "open",
      type: ["boolean"],
      default: "false",
      required: false,
      description: "Set the initial state of the disclosure.",
    },
    {
      name: "summary",
      type: ["ReactNode"],
      default: "",
      required: true,
      description: "Define the text to be displayed as the summary of the disclosure. This should generally be a short sentenc and related to the content",
    },
    {
      name: "padding",
      type: ["boolean"],
      default: "true",
      required: false,
      description: "Remove or add a padding to align the content with the icon indicator. This is useful when the content inside starts with text and you want to align it summary to improve readability.",
    },
    {
      name: "contentMaxHeight",
      type: ["string"],
      default: "",
      required: false,
      description: "Set the max height of the content after which the scrollbar will appear.",
    },
    {
      name: "dimension",
      type: ['enum'],
      typeValue: "'small' | 'regular' | 'big'",
      default: '"regular"',
      required: false,
      description: "Set the dimension of the summary toggle",
    },
    {
      name: "iconPosition",
      type: ['enum'],
      typeValue: "'left' | 'right'",
      default: '"left"',
      required: false,
      description: "Set the position of the icon indicator. The content padding is automatically applied based on the icon position.",
    },
    {
      name: "expandable",
      type: ["boolean"],
      default: "true",
      required: false,
      description: "Set or disable the interactivity of the summary toggle.Eg. If `open` is set to `true` the content can't be toggled.",
    },
    {
      name: "onToggle",
      type: ["function"],
      default: "",
      required: false,
      description: "Call a function when the element is opened or closed.",
    },
  ]}
/>

## Accessibility

Adheres to the [`disclosure` role requirements](https://www.w3.org/TR/wai-aria-practices-1.2/#disclosure).

<KeyTable
  data={[
    {
      name: "tab",
      description: "Move focus on the summary element or outside",
    },
    {
      name: "enter",
      description: "Expand or collapse the content",
    },
  ]}
/>
