import ComponentsNav from "@/data/components-nav";
import { getLayoutProps } from "@/utils/get-layout-props";
import { PropsTable } from "@/components/doc/props-table";
import { KeyTable } from "@/components/shared/key-table";

export const getStaticProps = () =>
  getLayoutProps({
    layout: "component",
    layoutProps: {
      title: "Icon button",
      color: "indigo",
      subtitle: "When the action a button invoke is so clear that needs no words, just an icon.",
      navigation: ComponentsNav,
      features: [
        "Keyboard accessible",
        "Focusable even when disabled",
        "Multiple styles and sizes",
        "Themes ready",
      ],
      links: [
        {
          label: "ARIA authoring",
          url: "https://www.w3.org/WAI/ARIA/apg/patterns/button/",
        },
      ]
    }
  });

<LiveArea>
  <IconButton icon="bell" />
</LiveArea>

## Anatomy

```tsx lines="1,4"
import { IconButton } from "@wonderflow/react-components";

export default () => {
  return <IconButton icon="plus">Click</IconButton>;
};
```

## API Reference

<PropsTable
  properties={[
    {
      name: "kind",
      type: ['enum'],
      typeValue: "ButtonProps['kind']",
      typeLink: "/get-started/components/actions/button#prop-kind",
      default: 'primary',
      required: false,
      description: "Set the style of the button. When the button is `disabled` this property is ignored.",
    },
    {
      name: "dimension",
      type: ['enum'],
      typeValue: "ButtonProps['dimension']",
      typeLink: "/get-started/components/actions/button#prop-dimension",
      default: 'regular',
      required: false,
      description: "Set the size of the button.",
    },
    {
      name: "icon",
      type: ["enum"],
      typeValue: "ButtonProps['icon']",
      typeLink: "/get-started/components/actions/button#prop-icon",
      default: "",
      required: false,
      description: "Define the icon to use",
    },
    {
      name: "disabled",
      type: ["boolean"],
      typeValue: "ButtonProps['disabled']",
      typeLink: "/get-started/components/actions/button#prop-disabled",
      default: "",
      required: false,
      description: "Set disabled state. The button is not interactive and grayed out.",
    },
    {
      name: "busy",
      type: ["boolean"],
      typeValue: "ButtonProps['busy']",
      typeLink: "/get-started/components/actions/button#prop-busy",
      default: "",
      required: false,
      description: "Set the loading state and show a spinner.",
    },
    {
      name: "pressed",
      type: ["boolean"],
      typeValue: "ButtonProps['pressed']",
      typeLink: "/get-started/components/actions/button#prop-pressed",
      default: "false",
      required: false,
      description: "Set the pressed state and add required aria attributes.",
    },
    {
      name: "onClick",
      type: ["function"],
      typeValue: "ButtonProps['onClick']",
      typeLink: "/get-started/components/actions/button#prop-onclick",
      default: "",
      required: false,
      description: "Callback function to be called when the button is pressed.",
    },
    {
      name: "as",
      type: ["enum"],
      typeValue: "keyof JSX.IntrinsicElements | React.ComponentType<any>",
      default: 'button',
      required: false,
      description:
        "Set the component to a different HTML tag or custom component",
    },
  ]}
/>

## Accessibility

Adheres to the [`button` role requirements](https://www.w3.org/TR/wai-aria-1.2/#button). This component always requires the `aria-label` or `aria-labelledby` attributes to describe the action.

### Keyboard interactions

<KeyTable
  data={[
    {
      name: "tab",
      description: "Move focus on the element or outside",
    },
    {
      name: "enter",
      description: "Trigger the component action",
    },
  ]}
/>
