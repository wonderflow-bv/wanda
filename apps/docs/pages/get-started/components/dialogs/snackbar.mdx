import ComponentsNav from "@/data/components-nav";
import { getLayoutProps } from "@/utils/get-layout-props";
import { PropsTable } from "@/components/doc/props-table";
import { KeyTable } from "@/components/shared/key-table";

export const getStaticProps = () =>
  getLayoutProps({
    layout: "component",
    layoutProps: {
      title: "Snackbar",
      color: "indigo",
      subtitle: "Highlight prominent messages",
      navigation: ComponentsNav,
      features: [
        "Keyboard accessible",
        "Customizable icon",
        "Multiple levels of severity",
        "Themes ready",
      ],
      links: [
        {
          label: "ARIA design",
          url: "https://www.w3.org/TR/wai-aria-1.2/#status",
        },
      ]
    }
  });

<LiveArea>
  <Snackbar dismissable>The Relevance section uses only Feedback with that can be analysed with NLP and have a score. <a href="#">Learn more</a></Snackbar>
</LiveArea>


## Anatomy

```tsx lines="1,4"
import { Snackbar } from "@wonderflow/react-components";

export default () => {
  return <Snackbar>...</Snackbar>;
};
```

## API Reference

<PropsTable
  properties={[
    {
      name: "icon",
      type: ["enum"],
      typeValue: "IconProps['source']",
      typeLink: "/components/other/icon#prop-source",
      default: "",
      required: false,
      description: "Set the icon to be displaye alongside the title. This icon have to enforce the message in a not misleading way.",
    },
    {
      name: "title",
      type: ["string"],
      default: "",
      required: false,
      description: "Set the title of the snackbar. This must concisely describe the message.",
    },
    {
      name: "kind",
      type: ['enum'],
      typeValue: "'info' | 'warning' | 'neutral' | 'positive' | 'danger'",
      default: 'neutral',
      required: false,
      description: "Set the color and the sentiment of the snackbar. This affects the the color of all the elements inside and should be defined according to the message.",
    },
    {
      name: "dismissable",
      type: ["boolean"],
      default: "",
      required: false,
      description: "Define if the snackbar can be dismissed by user interaction. If `true` a button will be displayed.",
    },
    {
      name: "dismissLabel",
      type: ["string"],
      default: 'Dismiss',
      required: false,
      description: "Set the label of the dismiss button.",
    },
    {
      name: "onDismiss",
      type: ["function"],
      typeValue: "() => void",
      default: "",
      required: false,
      dangerous: false,
      description: "Callback function to be called when the dismiss button is clicked.",
    },
    {
      name: "as",
      type: ["enum"],
      typeValue: "keyof JSX.IntrinsicElements | React.ComponentType<any>",
      default: 'output',
      required: false,
      description:
        "Set the component to a different HTML tag or custom component",
    }
  ]}
/>

## Accessibility

Adheres to the [`status` role requirements](https://www.w3.org/TR/wai-aria-1.2/#status).

The `Snackbar` component uses the element `output` by default with inplicits `aria-live="polite"` and `aria-atomic="true"` attributes. Add those to change the default values.

### Keyboard interactions
<KeyTable
  data={[
    {
      name: "tab",
      description: "Move focus on the first focusable element inside",
    },
  ]}
/>
