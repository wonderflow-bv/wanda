import ComponentsNav from "@/data/components-nav";
import { getLayoutProps } from "@/utils/get-layout-props";
import { PropsTable } from "@/components/doc/props-table";
import { KeyTable } from "@/components/shared/key-table";

export const getStaticProps = () =>
  getLayoutProps({
    layout: "component",
    layoutProps: {
      title: "Tooltip",
      color: "indigo",
      subtitle: "A popup that displays information related to an element.",
      navigation: ComponentsNav,
      features: [
        "Opens when the trigger is focused or hovered.",
        "Closes when the trigger is activated or when pressing escape.",
        "Supports custom timing.",
      ],
      links: [
      {
        label: "ARIA design",
        url: "https://www.w3.org/TR/wai-aria-1.2/#tooltip",
      },
      {
        label: "ARIA authoring",
        url: "https://www.w3.org/WAI/ARIA/apg/patterns/tooltip/",
      },
    ]
    }
  });

<LiveArea>
  <Tooltip trigger={<span>Hover me</span>}>This is an extended description.</Tooltip>
</LiveArea>


## Anatomy

```tsx lines="1,5-7"
import { Tooltip } from "@wonderflow/react-components";

export default () => {
  return (
    <Tooltip trigger={<span tabIndex={0}>Hover me</span>}>
      This is an extended description.
    </Tooltip>
  );
};
```

## API Reference

<PropsTable
  properties={[
    {
      name: "trigger",
      type: ['ReactNode'],
      default: "",
      required: true,
      description: "Set the element that will be used as the trigger of the tooltip.",
    },
    {
      name: "placement",
      type: ["enum"],
      typeValue: "PopoverProps['placement']",
      typeLink: "/get-started/components/dialogs/popover#prop-placement",
      default: "auto-start",
      required: false,
      description:
        "The placement of the popover. This is automatically handled based on scroll and viewport edges. By default the popover is anchored at the beginning of the trigger. The first key refers to the X axis, the second key refers to the Y axis. Eg: `auto-start` means the popover will be placed automatically on left or right based on the available space, and anchored at the top (start) of the trigger.",
    },
    {
      name: "open",
      type: ["boolean"],
      default: "",
      required: false,
      description:
        "Set if the tooltip should be shown or hidden.",
    },
    {
      name: "delay",
      type: ["number"],
      default: "500",
      required: false,
      description: "Define a delay time in milliseconds before the tooltip is shown.",
    },
    {
      name: "maxWidth",
      type: ['string'],
      default: '40ch',
      required: false,
      description: "Set the max width of the tooltip. This prevents the tooltip content from filling the whole screen.",
    },
    {
      name: "interactive",
      type: ["boolean"],
      default: "false",
      required: false,
      description: "Make the tooltip interactive. This will allow the user to interact with the tooltip content.",
    },
    {
      name: "fill",
      type: ["boolean"],
      default: "false",
      required: false,
      description: "Set the trigger container to behave like inline or block element",
    },
  ]}
/>

## Accessibility

Adheres to the [`tooltip` role requirements](https://www.w3.org/TR/wai-aria-1.2/#tooltip).

### Keyboard interactions

<KeyTable
  data={[
    {
      name: "tab",
      description: "Move focus on the trigger and open the tooltip.",
    },
    {
      name: "esc",
      description: "Close the tooltip",
    },
  ]}
/>
