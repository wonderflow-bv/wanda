import ComponentsNav from "@/data/components-nav";
import { getLayoutProps } from "@/utils/get-layout-props";
import { PropsTable } from "@/components/doc/props-table";
import tkns from "@wonderflow/tokens/platforms/web/tokens.json";

export const getStaticProps = () =>
  getLayoutProps({
    layout: "component",
    layoutProps: {
      title: "Card",
      color: "indigo",
      subtitle: "Wrap your content with style",
      navigation: ComponentsNav,
      features: [
        "Highly customizable",
        "Multiple slots",
        "Polymorphic"
      ]
    }
  });

<LiveArea>
  <Card
    left={<Symbol source="message" dimension={32} />}
    right={<Symbol source="compass" dimension={32} />}
    vibrant
  >
    Lorem ipsum dolor sit, amet consectetur adipisicing elit. Eius
    dolores, tempore quas labore officiis praesentium. Porro sed dolorem,
    numquam temporibus.
  </Card>
</LiveArea>

## Anatomy

```tsx lines="1,5-10"
import { Card } from "@wonderflow/react-components";

export default () => {
  return (
    <Card
      left={<Symbol source="message" />}
      right={<Symbol source="compass" />}
    >
      Lorem ipsum dolor sit.
    </Card>
  );
};
```

## API Reference

<PropsTable
  properties={[
    {
      name: "wrap",
      type: ["boolean"],
      typeValue: "StackProps['wrap']",
      typeLink: "/get-started/components/layouts/stack#prop-wrap",
      default: "false",
      required: false,
      description: "Wrap `left`, `children` and `right` there is no space for them",
    },
    {
      name: "columnGap",
      type: "enum",
      typeValue: "StackProps['columnGap']",
      typeLink: "/get-started/components/layouts/stack#prop-columngap",
      default: "24",
      required: false,
      description: "Set the space between `left`, `children` and `right`",
    },
    {
      name: "vAlign",
      type: ["string"],
      typeValue: "StackProps['vAlign']",
      typeLink: "/get-started/components/layouts/stack#prop-vAlign",
      default: "start",
      required: false,
      description:
        "Set the vertical content placement instead of growing to fill the available space.",
    },
    {
      name: "hAlign",
      type: ["string"],
      typeValue: "StackProps['hAlign']",
      typeLink: "/get-started/components/layouts/stack#prop-hAlign",
      default: "space-between",
      required: false,
      description:
        "Set the horizontal content placement instead of growing to fill the available space.",
    },
    {
      name: "padding",
      type: ["enum"],
      typeValue: `false | ${Object.keys(tkns.space).map(name => `${name}`).join(" | ")}`,
      default: "24",
      required: false,
      dangerous: false,
      description: "Set the padding or remove it",
    },
    {
      name: "left",
      type: ["ReactNode"],
      default: "",
      required: false,
      description: "ontent rendered inside the card on the left side, before children.",
    },
    {
      name: "right",
      type: ["ReactNode"],
      default: "",
      required: false,
      description: "Content rendered inside the card on the right side, after children.",
    },
    {
      name: "bordered",
      type: ["boolean"],
      default: "",
      required: false,
      description: "Add a border to the card to increase its visual weight and contrast.",
    },
    {
      name: "radius",
      type: ["enum"],
      typeValue: `false | ${Object.keys(tkns.radius).map(name => `${name}`).join(" | ")}`,
      default: "8",
      required: false,
      description: "Define the edge radius of the card.",
    },
    {
      name: "dimmed",
      type: ["enum"],
      typeValue: "0 | 1 | 2",
      default: "",
      required: false,
      description: "Change the background color of the card.",
    },
    {
      name: "vibrant",
      type: ["boolean"],
      default: "",
      required: false,
      description: "Make the card vibrant. Add tranlucent background.",
    },
    {
      name: "highlightOnHover",
      type: ["boolean"],
      default: "",
      required: false,
      description:
        "Change the background color of the card when it is hovered.",
    },
    {
      name: "as",
      type: ["enum"],
      typeValue: "keyof JSX.IntrinsicElements | React.ComponentType<any>",
      default: 'div',
      required: false,
      description:
        "Set the component to a different HTML tag or custom component",
    }
  ]}
/>
