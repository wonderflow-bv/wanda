import ComponentsNav from "@/data/components-nav";
import { getLayoutProps } from "@/utils/get-layout-props";
import { PropsTable } from "@/components/doc/props-table";
import { useMemo } from 'react'
import tkns from "@wonderflow/tokens/platforms/web/tokens.json";

export const getStaticProps = () =>
  getLayoutProps({
    layout: "component",
    layoutProps: {
      title: "Stack",
      color: "indigo",
      subtitle: "Elements placing and spacing made easy",
      navigation: ComponentsNav,
      features: [
        "Content placement made easy",
        "Flex based",
        "Fully controllable",
        "Nestable",
      ]
    }
  });


<LiveArea>
  <Stack
    columnGap={8}
    direction="row"
    hAlign="center"
    fill={false}
  >
    <Select>
      <option value="1">Option 1</option>
      <option value="2">Option 2</option>
      <option value="3">Option 3</option>
    </Select>
    <Button>Confirm</Button>
  </Stack>
</LiveArea>

## Anatomy

```tsx lines="1,5-14"
import { Stack } from "@wonderflow/react-components";

export default () => {
  return (
    <Stack
      columnGap={8}
      direction="row"
      hAlign="center"
      fill={false}
      wrap
    >
      <Select>...</Select>
      <Button>Confirm</Button>
    </Stack>
  );
};
```

## API Reference

<PropsTable
  properties={[
    {
      name: "rowGap",
      type: ["TokensTypes['space']"],
      typeValue: `${Object.keys(tkns.space).map(name => `${name}`).join(" | ")}`,
      default: "0",
      required: false,
      description: "Add a gap between rows.",
    },
    {
      name: "columnGap",
      type: ["TokensTypes['space']"],
      typeValue: `${Object.keys(tkns.space).map(name => `${name}`).join(" | ")}`,
      default: "0",
      required: false,
      description: "Set the space between columns",
    },
    {
      name: "inline",
      type: ["boolean"],
      default: "false",
      required: false,
      description: "Display the element as `inline-flex`",
    },
    {
      name: "wrap",
      type: ["boolean"],
      default: "false",
      required: false,
      description: "Wrap children whene there is no space for them",
    },
    {
      name: "fill",
      type: ["boolean"],
      default: "true",
      required: false,
      description: "Make the children grow to fill the available space instead of being sized based on their content.",
    },
    {
      name: "vAlign",
      type: ["string"],
      default: "",
      required: false,
      description:
        "Set the vertical content placement instead of growing to fill the available space.",
    },
    {
      name: "hAlign",
      type: ["string"],
      default: "",
      required: false,
      description:
        "Set the horizontal content placement instead of growing to fill the available space.",
    },
    {
      name: "hPadding",
      type: ["TokensTypes['space']"],
      typeValue: `${Object.keys(tkns.space).map(name => `${name}`).join(" | ")}`,
      default: "0",
      required: false,
      description: "Set the horizontal padding (left/right)",
    },
    {
      name: "vPadding",
      type: ["TokensTypes['space']"],
      typeValue: `${Object.keys(tkns.space).map(name => `${name}`).join(" | ")}`,
      default: "0",
      required: false,
      description: "Set the vertical padding (top/bottom)",
    },
    {
      name: "direction",
      type: ['enum'],
      typeValue: "'row' | 'column' | 'row-reverse' | 'column-reverse'",
      default: '"column"',
      required: false,
      description:
        "Renderes children as rows or columns. The value can be one of the flex directions. More info: [https://developer.mozilla.org/en-US/docs/Web/CSS/flex-direction](https://developer.mozilla.org/en-US/docs/Web/CSS/flex-direction)",
    },
    {
      name: "maxWidth",
      type: ['string'],
      default: '',
      required: false,
      description:
        "Set a max width for the stack container.",
    },
    {
      name: "as",
      type: ["enum"],
      typeValue: "keyof JSX.IntrinsicElements | React.ComponentType<any>",
      default: 'div',
      required: false,
      description:
        "Set the component to a different HTML tag or custom component",
    }
  ]}
/>
