import ComponentsNav from "@/data/components-nav";
import { getLayoutProps } from "@/utils/get-layout-props";
import { PropsTable } from "@/components/doc/props-table";

export const getStaticProps = () =>
  getLayoutProps({
    layout: "component",
    layoutProps: {
      title: "Circular progress",
      color: "indigo",
      subtitle: "Indicates determinate loading status",
      navigation: ComponentsNav,
      features: [
        "Multiple sizes",
        "Themes ready"
      ],
      links: [
        {
          label: "ARIA design",
          url: "https://www.w3.org/TR/wai-aria-1.2/#progressbar",
        },
      ]
    }
  });

<LiveArea>
  <CircularProgress value={70} showProgress />
</LiveArea>

## Anatomy

```tsx lines="1,4"
import { CircularProgress } from "@wonderflow/react-components";

export default () => {
  return <CircularProgress value={70} />;
};
```

## API Reference

<PropsTable
  properties={[
    {
      name: "value",
      type: ["number"],
      default: "",
      required: true,
      description: "Set the current progress of the progress bar. This value should be between 0 and 'max'. The percentage is automatically computed.",
    },
    {
      name: "max",
      type: ["number"],
      default: "100",
      required: false,
      description: "Set the max value of the progress bar. This determines the computed percentage.",
    },
    {
      name: "dimension",
      type: ['enum'],
      typeValue: "'small' | 'regular' | 'big'",
      default: 'regular',
      required: false,
      description: "Set the dimension of the progress bar.",
    },
    {
      name: "showProgress",
      type: ["boolean"],
      default: "",
      required: false,
      description: "Show or hide the progress value.",
    },
    {
      name: "showPercentSign",
      type: ["boolean"],
      default: "false",
      required: false,
      description: "Show or hide the percent sign next to the value.",
    },
  ]}
/>

## Accessibility

Adheres to the [`progressbar` role requirements](https://www.w3.org/TR/wai-aria-1.2/#progressbar).
