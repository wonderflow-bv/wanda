import ComponentsNav from "@/data/components-nav";
import { getLayoutProps } from "@/utils/get-layout-props";
import { PropsTable } from "@/components/doc/props-table";

export const getStaticProps = () =>
  getLayoutProps({
    layout: "component",
    layoutProps: {
      title: "Spinner",
      color: "indigo",
      subtitle: "Animated indicator for loading and waiting states",
      navigation: ComponentsNav,
      features: [
        "Scalable vector graphic",
        "Allow custom color",
        "Multiple sizes",
        "Themes ready",
      ],
    }
  });

<LiveArea>
  <Spinner />
</LiveArea>

## Anatomy

```tsx lines="1,4"
import { Spinner } from "@wonderflow/react-components";

export default () => {
  return <Spinner />;
};
```
## API Reference

<PropsTable
  properties={[
    {
      name: "dimension",
      type: ['enum'],
      typeValue: "'small' | 'regular' | 'big'",
      default: 'big',
      required: false,
      description: "Set the size of the spinner",
    },
    {
      name: "color",
      type: ["string"],
      default: "",
      required: false,
      description: "Override the color of the spinner",
    },
  ]}
/>

