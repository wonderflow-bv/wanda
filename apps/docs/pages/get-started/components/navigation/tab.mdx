import ComponentsNav from "@/data/components-nav";
import { getLayoutProps } from "@/utils/get-layout-props";
import { PropsTable } from "@/components/doc/props-table";
import { KeyTable } from "@/components/shared/key-table";
import { Tab } from '@wonderflow/react-components'

export const getStaticProps = () =>
  getLayoutProps({
    layout: "component",
    layoutProps: {
      title: "Tab",
      color: "indigo",
      subtitle: "A set of layered sections of content — known as tab panels — that are displayed one at a time.",
      navigation: ComponentsNav,
      features: [
        "Keyboard accessible",
        "Supports automatic activation",
        "Themes ready",
      ],
      links: [
        {
          label: "ARIA design",
          url: "https://www.w3.org/TR/wai-aria-1.2/#tab",
        },
        {
          label: "ARIA authoring",
          url: "https://www.w3.org/WAI/ARIA/apg/patterns/tabpanel/",
        },
      ]
    }
  });


<LiveArea>
  <Elevator resting={1}>
    <Card padding={false} bordered>
      <Tab defaultValue="signup">
        <Tab.Panel icon="check" label="Sign-up" value="signup">
          <Card>
            <Stack rowGap={24}>
              <Textfield label="Name" type="text" />
              <Textfield label="E-Mail" type="email" />
              <Textfield
                label="Password"
                type="password"
                autoComplete="new-password"
                id="new-password"
              />
              <Button>Submit</Button>
            </Stack>
          </Card>
        </Tab.Panel>
        <Tab.Panel icon="envelope" label="Login" value="login">
          <Card>
            <Stack rowGap={24}>
              <Textfield label="E-Mail" type="email" />
              <Textfield
                label="Password"
                type="password"
                autoComplete="new-password"
                id="new-password"
              />
              <Button>Submit</Button>
            </Stack>
          </Card>
        </Tab.Panel>
      </Tab>
    </Card>
  </Elevator>
</LiveArea>


## Anatomy

```tsx lines="1,5-8"
import { Tab } from "@wonderflow/react-components";

export default () => {
  return (
    <Tab>
      <Tab.Panel value="1" label="Tab item 1">Tab panel 1</Tab.Panel>
      <Tab.Panel value="2" label="Tab item 2">Tab panel 2</Tab.Panel>
    </Tab>
  );
};
```

## API Reference

### Tab

Contains all the tabs component parts.

<PropsTable
  properties={[
    {
      name: "defaultValue",
      type: ["string"],
      default: "",
      required: false,
      description: "Set the value of the tab to be selected on the first render.",
    },
    {
      name: "onValueChange",
      type: ["function"],
      typeValue: "(value: string) => void",
      default: "",
      required: false,
      description:
        "Callback function called when the selected tab changes.",
    },
    {
      name: "dir",
      type: ["enum"],
      typeValue: "'ltr' | 'rtl'",
      default: "ltr",
      required: false,
      description:
        "The direction of navigation between toolbar items.",
    },
    {
      name: "activationMode",
      type: ["enum"],
      typeValue: "'automatic' | 'manual'",
      default: "automatic",
      required: false,
      description:
        "Whether a tab is activated automatically or manually.",
    },
    {
      name: "dimension",
      type: ["enum"],
      typeValue: "'regular' | 'big'",
      default: "'regular'",
      required: false,
      description:
        "Set the size of the tabs in the list.",
    },
    {
      name: "loop",
      type: ["boolean"],
      default: "true",
      required: false,
      description:
        "When true, keyboard navigation will loop from last tab to first, and vice versa.",
    },
  ]}
/>

### Tab.Panel

Contains the content associated with each trigger.

<PropsTable
  properties={[
    {
      name: "value",
      type: ["string"],
      default: "",
      required: true,
      description: "Set the value of the tab to use as a unique identifier.",
    },
    {
      name: "label",
      type: ["string"],
      default: "",
      required: false,
      description: "Set the tab panel name and accessible label",
    },
    {
      name: "symbol",
      type: ["enum"],
      typeValue: "SymbolProps['source']",
      typeLink: "/documentation/components/other/symbol#prop-source",
      default: "",
      required: false,
      description: "Add an additional symbol to the tab panel label, enforcing the meaning of the name and context.",
    },
  ]}
/>

## Accessibility

Adheres to the [Tabs WAI-ARIA design pattern](https://www.w3.org/WAI/ARIA/apg/patterns/tabpanel/).

### Keyboard interactions

<KeyTable
  data={[
    {
      name: "tab",
      description:
        "When focus moves onto the tabs, focuses the active trigger. When a trigger is focused, moves focus to the active content",
    },
    {
      name: "ArrowRight",
      description:
        "Moves focus to the next trigger and activates its associated content",
    },
    {
      name: "ArrowLeft",
      description:
        "Moves focus to the previous trigger and activates its associated content",
    },
  ]}
/>
