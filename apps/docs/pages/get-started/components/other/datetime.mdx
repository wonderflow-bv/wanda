import ComponentsNav from "@/data/components-nav";
import { getLayoutProps } from "@/utils/get-layout-props";
import { PropsTable } from "@/components/doc/props-table";

export const getStaticProps = () =>
  getLayoutProps({
    layout: "component",
    layoutProps: {
      title: "Datetime",
      color: "indigo",
      subtitle: "Format dates and times in a smart way",
      navigation: ComponentsNav,
      features: [
        "Native options",
        "Uses the correct semantic markup",
        "Supports ISO date formats",
      ]
    }
  });

<LiveArea>
  <Datetime date={Date.now()} locale="it-IT" options={{ year: '2-digit' }} />
</LiveArea>

## Anatomy

```tsx lines="1,4"
import { Datetime } from "@wonderflow/react-components";

export default () => {
  return <Datetime date="" />;
};
```

## API Reference

<PropsTable
  properties={[
    {
      name: "date",
      type: ["string"],
      default: "",
      required: true,
      description: "The date to format and display.",
    },
    {
      name: "locale",
      type: ["string"],
      default: 'en-US',
      required: false,
      description: "Set the locale to use to format the date.",
    },
    {
      name: "options",
      type: ["Intl.DateTimeFormatOptions"],
      default: "",
      required: false,
      description:
        "All the options available in the [Intl.DateTimeFormat](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Intl/DateTimeFormat/DateTimeFormat)",
    },
  ]}
/>
