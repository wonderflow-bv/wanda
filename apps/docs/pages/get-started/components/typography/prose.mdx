import ComponentsNav from "@/data/components-nav";
import { getLayoutProps } from "@/utils/get-layout-props";
import { PropsTable } from "@/components/doc/props-table";
import tkns from "@wonderflow/tokens/platforms/web/tokens.json";

export const getStaticProps = () =>
  getLayoutProps({
    layout: "component",
    layoutProps: {
      title: "Prose",
      color: "indigo",
      subtitle: "Vertical rhythm for text content",
      navigation: ComponentsNav,
      features: [
        "Auto space titles and text content",
        "Useful for pages with many text elements",
      ]
    }
  });

<LiveArea>
  <Prose>
  <Title as="h1" level="4">
    Title
  </Title>
  <Text>
    Lorem ipsum dolor sit, amet consectetur adipisicing elit. Eius
    dolores, tempore quas labore officiis praesentium.
  </Text>
</Prose>
</LiveArea>

## Anatomy

```tsx lines="1,4"
import { Prose } from "@wonderflow/react-components";

export default () => {
  return <Prose>...</Prose>;
};
```

## API Reference

<PropsTable
  properties={[
    {
      name: "size",
      type: ["enum"],
      typeValue: `${Object.keys(tkns.font.size).map(name => `${name}`).join(" | ")}`,
      default: "",
      required: false,
      description: "Set the dimension of the text from one of the typography system values",
    },
    {
      name: "sentiment",
      type: ["enum"],
      typeValue: "'positive' | 'informative' | 'danger' | 'warning'",
      default: "",
      required: false,
      description:
        "et the sentiment of the text.",
    },
    {
      name: "dimmed",
      type: ["enum"],
      typeValue: '5 | 6 | 7',
      default: "",
      required: false,
      description: "Set the dimmed color of the text. To keep readability and contrast, you can only use dimmed colors `5`, `6`, and `7`.",
    },
    {
      name: "weight",
      type: ["enum"],
      typeValue: "'thin' | 'bold'",
      default: "",
      required: false,
      description: "Set the font weight of the text. The values are consistent with the typography system.",
    },
    {
      name: "maxWidth",
      type: ["string"],
      default: "",
      required: false,
      description:
        "Set the maximum width of the text after which it will wrap.",
    },
    {
      name: "textAlign",
      type: ["enum"],
      typeValue: "'start' | 'center' | 'end'",
      default: "start",
      required: false,
      description:
        "Set the text alignment of the text. This is a logical property based on the direction of the text.",
    },
    {
      name: "responsive",
      type: ["boolean"],
      default: "true",
      required: false,
      description:
        "Enable or disable the responsiveness of the text. If disabled, the text will be always the same size across all breakpoints.",
    },
    {
      name: "lineHeight",
      type: ['enum'],
      typeValue: "'none' | 'small' | 'large'",
      default: 'large',
      required: false,
      description: "Set the text line-height of the text. This uses the predefined tokens from the typography system.",
    },
    {
      name: "as",
      type: ["enum"],
      typeValue: "keyof JSX.IntrinsicElements | React.ComponentType<any>",
      default: 'p',
      required: false,
      description:
        "Set the component to a different HTML tag or custom component",
    }
  ]}
/>

## Accessibility

The `Title` component is rendered as `span` by default. Make sure to define the correct heading tag when semantic is required to make the content navigable and accessible with AT.

Read more about how to [manage headings and accessibility](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/Heading_Elements#accessibility_concerns).
