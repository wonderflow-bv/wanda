import ComponentsNav from "@/data/components-nav";
import { getLayoutProps } from "@/utils/get-layout-props";
import { PropsTable } from "@/components/doc/props-table";
import { KeyTable } from "@/components/shared/key-table";

export const getStaticProps = () =>
  getLayoutProps({
    layout: "component",
    layoutProps: {
      title: "Chip",
      color: "indigo",
      subtitle: "Dismissable tag elements.",
      navigation: ComponentsNav,
      features: [
        "Keyboard accessible",
        "Multiple styles and sizes",
        "Dismissable",
        "Themes ready",
      ]
    }
  });

<LiveArea>
  <Chip color="blue" interactive>Chip text</Chip>
</LiveArea>


## Anatomy

```tsx lines="1,4"
import { Chip } from "@wonderflow/react-components";

export default () => {
  return <Chip>Text</Chip>;
};
```

## API Reference

<PropsTable
  properties={[
    {
      name: "dimension",
      type: ['enum'],
      typeValue: "'small' | 'regular' | 'big'",
      default: 'regular',
      required: false,
      description: "Set the dimension of the component.",
    },
    {
      name: "color",
      type: ["enum"],
      typeValue: "'blue' | 'cyan' | 'dipsy' | 'gray' | 'green' | 'indigo' | 'magenta' | 'purple' | 'red' | 'salmon' | 'violet' | 'yellow'",
      default: 'gray',
      required: false,
      description: "Set a color from one of the provided values.",
    },
    {
      name: "icon",
      type: ["enum"],
      typeValue: "SymbolProps['source']",
      typeLink: "/get-started/components/widgets/symbol#prop-source",
      default: "",
      required: false,
      description: "Show an icon before the chip content. The icon is not rendered if `dismissable` is `true`",
    },
    {
      name: "interactive",
      type: ["boolean"],
      default: "",
      required: false,
      description: "Make the chip dismissable. When `true` adds a close button on the side.",
    },
    {
      name: "onDismissClick",
      type: ["function"],
      typeValue: "() => void",
      default: "",
      required: false,
      description: "Callback function to be called when the dismiss button is pressed.",
    },
  ]}
/>

## Accessibility

### Keyboard interactions

<KeyTable
  data={[
    {
      name: "tab",
      description: "If interactive, move focus on the button element inside",
    }
  ]}
/>
