query Mentions {
  mentions(stage: PUBLISHED, orderBy: updatedAt_DESC) {
    id
    author {
      name
      handle
      image {
        url(
          transformation: { image: {}, document: { output: { format: webp } } }
        )
      }
    }
  }
}
