query resources {
  resourceGroups {
    id
    groupName
    groupSymbol
    groupColor
    resources {
      ... on Tile {
        id
        title
        description
        symbol
        symbolColor
        url
      }
    }
  }
}
